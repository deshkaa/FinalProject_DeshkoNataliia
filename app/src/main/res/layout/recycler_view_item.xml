<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_margin="@dimen/recycler_item_margin">

    <ImageView
        android:id="@+id/recycler_image_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="left"
        android:adjustViewBounds="true" />

    <TextView
        android:id="@+id/recycler_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/recycler_image_view"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_margin="@dimen/recycler_text_view_padding"
        android:textColor="@color/colorRecyclerCaptions"
        android:textSize="@dimen/recycler_view_caption_size"
        android:textStyle="italic" />

</RelativeLayout>